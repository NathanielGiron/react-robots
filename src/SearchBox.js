import React, { Fragment } from 'react';

const SearchBox = () => {
  return(
    <Fragment>
      <input type='search' placeholder="Search Robots" />
    </Fragment>
  );
}

export default SearchBox;